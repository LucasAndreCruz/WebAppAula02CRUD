@model IEnumerable<WebAppAula02CRUD.Models.Estudante>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CPF)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CPF)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstudanteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstudanteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstudanteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
